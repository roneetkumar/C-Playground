#include "myFunctions.h"

using namespace std;

main()
{
    //variables
    int no;

    displayText("Enter Value For Prime No's  : ");
    int NB = readNo();

    int num1 = readNo();
    int num2 = readNo();

    for (short i = 1; i < NB; i++)
    {
        if()
    }
}